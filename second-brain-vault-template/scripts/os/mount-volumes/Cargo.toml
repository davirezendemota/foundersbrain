[package]
name = "mount-volumes"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mount-volumes"
path = "src/main.rs"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
