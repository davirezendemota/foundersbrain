# Credenciais de IA ficam no vault: {vault}/.second-brain/api-credentials.enc
# Configure pela UI do app apos selecionar o vault.

# Opcional: URL de backend legado (content library, etc.)
# VITE_BACKEND_URL=http://localhost:10001
